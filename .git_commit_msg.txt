docs: add PostgreSQL existing-database setup guide to README

Covers connecting SourceDesk's server to an already-running Postgres
instance (native/bare-metal, Docker container, or rootless Podman)
without spinning up a new one via docker-compose.

README.md
- New `### Using an existing PostgreSQL database` section between
  Email Ingest API and Running with Docker:
  - Connection string format table with privilege and URL-encoding notes
  - Scenario A: native Postgres (localhost); Homebrew tip; pgvector tip
  - Scenario B: existing Docker container -- host-side Node.js path
    (localhost + published port) and container-to-container path
    (shared network by name, host.docker.internal, Linux 172.17.0.1 /
    --add-host workarounds)
  - Scenario C: rootless Podman -- host.docker.internal unavailable;
    bridge IP (10.0.2.2) workaround documented
  - First-run migrations: idempotent, auto-run on startup, npm run
    migrate for manual runs
  - Recommended Postgres role setup SQL block with optional
    CREATE EXTENSION vector for future RAG support
- `### Local server setup` .env snippet now shows SQLite and Postgres
  DATABASE_URL options side-by-side with install hints; npm run migrate
  note added below the snippet

CHANGELOG.md  -- [Unreleased] 🖥️ Changed entry added
CLAUDE.md     -- current session note updated
docs/feature-request-hindsight.md -- new feature-request note (Hindsight
  pgvector integration)
